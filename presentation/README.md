# VAC Bot Presentation — presentation/

Files:
- `vac_bot_slides.md` — Slide content and speaker notes (source)
- `export_pptx.sh` — Small helper to convert the markdown to a `.pptx` using `pandoc`

Quick export (requires `pandoc`):

```bash
cd presentation
./export_pptx.sh
```

Alternative: convert with Python using `python-pptx` for more styling control.

Next steps I can do for you:
- Export and attach a `.pptx` file here (I can generate using `python-pptx`).
- Add suggested visuals/icons per slide.
- Tweak tone for investors, engineers, or customers.
